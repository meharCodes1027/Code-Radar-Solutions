#include <stdio.h>
int main() {
    int a, b, c;
    float average;
    scanf("%d", &a);
      scanf("%d", &b);
        scanf("%d", &c);
          average = (num1 + num2 + num3) / 3.0;
    printf(" Average: %.2f\n", average);
    return 0;
}